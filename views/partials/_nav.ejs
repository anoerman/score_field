<nav class="navbar">
    <a href="/" class="navbar-logo">Scoreboard</a>
    <ul class="navbar-links">
        <li><a href="/">Beranda</a></li>
        <% if (user) { %>
            <li><a href="/matches">Data Pertandingan</a></li>
            <% if (user.isAdmin) { %>
                <li><a href="/game-presets">Preset Permainan</a></li>
                <% } %>
                    <form action="/auth/logout" method="post" style="display:inline">
                        <li><button type="submit" class="navbar-button">Logout (<%= user.username %>)</button></li>
                    </form>
                    <% } else { %>
                        <li><a href="/auth/login">Login</a></li>
                        <li><a href="/auth/register">Daftar</a></li>
                        <% } %>
    </ul>
</nav>
<% if (flash) { %>
    <div class="alert-container">
        <div class="alert <%= flash.type %>">
            <%= flash.message %>
        </div>
    </div>
    <% } %>